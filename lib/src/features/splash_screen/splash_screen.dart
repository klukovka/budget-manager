import 'package:flutter/material.dart';

class SplashScreen extends StatelessWidget {
  const SplashScreen({Key? key}) : super(key: key);

  static String get screenName => 'splash_screen';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
