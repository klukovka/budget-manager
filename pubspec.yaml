name: budget_manager
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^7.1.0
  equatable: ^2.0.3

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter
  test: ^1.16.8

flutter:
  uses-material-design: true
  fonts:
    - family: Helvetica
      fonts:
        - asset: "assets/helvetica.ttf"
        - asset: "assets/helvetica_bold.ttf"
